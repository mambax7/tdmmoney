<h5>1.2 Beta 1                          [2017-05-24]</h5>
<hr>
- removed @version (mamba)
- removed TDMMoney_checkModuleAdmin() checks (mamba)
- PSR-2 code cosmetics (mamba)
- displayNavigation(basename(__FILE__))  (mamba)
- dirname(__FILE__) to __DIR__ (mamba)
- XOOPS 2.5.8, PHP 7 (mamba)
- replaced < br /> with < br > (mamba)
- Unnecessary double quotes (mamba)
- reference mismatch (mamba)
- Hardening to type safe (mamba)
- update Help (mamba)
- changed _handler to Handler (mamba)
- changed to ->displayButton('left') (mamba)
- HTML 5: removed 'selected' and 'checked' (mamba)
- moved all images, CSS, and JS files to /assets (mamba)
- renamed .html Smarty templates to .tpl (mamba)
- removed PHP4 constructors (mamba)
- started conversion to XMF (mamba)
- added optional Load Sample Data button (mamba)
- made PDF button optional (mamba)
- fix XoopsFormSelect bug (geekwright)
- made stats numbers bold (mamba)
- fixed bugs in install script (zap_english/mamba)
- fixed bugs with '<{$module_url}>/' (mamba)
- added blank.png (mamba)
- fix sub-category bug (XoopsFormSelect) (Zap_English/geekwright)
- update README.md (zyspec)
- update XOOPS Foundation PAYPAL link (zyspec)
- filter POST input in admin (zyspec)
- check to see if category isNew() when setting parent category (zyspec)
- fix render of category select box in XOOPS 2.5.9+ (zyspec)
- updated reference from "SubCategory" to "Parent Category" (zyspec)
- changed _MD_TDMMONEY_ERROR_NO_PDF to _AM_TDMMONEY_ERROR_NO_PDF and moved to Admin language file (zyspec)
- updated Help file (zyspec)

<h5>1.1         [2011-01-11]</h5>
<hr>
List of bugs:

 - Bug with redirection (Mage).
 - It was impossible to edit an operation in the administration (Mage).
 - Utf-8 characters not displayed correctly in PDF (Mage).

List of improvements:

 - Administration area has be recoded to be 100% compatible with xoops 2.5 (Mage).

Informations

 - See the file "lang_diff.txt" for changes in language.

Installation:

 1 - You have to delete the existent folder "TDMDownloads" in "modules".
 2 - Upload the new folder  "TDMMoney"   to your server in the "modules" folder.
 3 - Update the module.


<h5>1.01       [2010-03-27]</h5>
<hr>

List of bugs:

 - Bug in the size of certain fields in the database (Mage).
